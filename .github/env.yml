# Centralized Environment Configuration
# This file defines version constants used across all CI workflows
# Update versions here to maintain consistency across the pipeline

# Tool Versions
TF_VERSION: "1.12.2"
TFLINT_VERSION: "v0.55.0"
TERRAFORM_DOCS_VERSION: "v0.17.0"
CHECKOV_VERSION: "3.2.0"
TRIVY_VERSION: "0.50.0"
TERRASCAN_VERSION: "1.19.1"
KUBECTL_VERSION: "v1.28.0"
HELM_VERSION: "v3.13.0"

# Kubernetes Configuration
MICROK8S_CHANNEL: "1.28/stable"

# CI Configuration
CI: "true"
GITHUB_ACTIONS: "true"

# Timeout Configuration (minutes)
QUICK_TIMEOUT: 5
VALIDATION_TIMEOUT: 10
SECURITY_TIMEOUT: 15
TEST_TIMEOUT: 20
RELEASE_TIMEOUT: 30