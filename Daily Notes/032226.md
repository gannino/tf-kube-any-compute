# 032226 - Daily Work Log

## Decisions

- Use complete Terraform template files for complex YAML instead of heredoc interpolation
- Remove TTL from Kubernetes Jobs managed by Terraform to prevent recreation loops
- Implement dynamic node discovery instead of hardcoded names for cluster-agnostic deployments

## Meetings & Conversations

## Notes

### Rook Ceph Troubleshooting Session

**Issues Fixed:**
1. Template interpolation error - `${nodes_yaml}` being interpreted as resource reference
2. Cleanup jobs recreating on every apply due to `ttl_seconds_after_finished = 60`
3. Hardcoded node names - replaced with dynamic discovery via `data.kubernetes_nodes`

**Current Status:**
- Cluster deployed with `ssl: false` for dashboard
- Mon pods pending (scheduler anti-affinity on raspberrypi3)
- Waiting for OSDs to be created
- User will confirm when deployment progresses

**Files Modified:**
- `helm-rook-ceph/ceph-cluster.tf` - Simplified using templatefile()
- `helm-rook-ceph/cleanup-stale-data.tf` - Removed TTL
- `helm-rook-ceph/templates/ceph-cluster.yaml.tpl` - New complete template

## End of Day Summary

**Accomplishments:**

- Fixed Rook Ceph template interpolation by creating complete YAML template
- Fixed cleanup jobs recreation issue (removed TTL)
- Implemented dynamic node discovery for storage configuration
- Set up Claudify workflow system

**Open Items:**

- Rook Ceph cluster stabilization (mon/OSD pods pending)
- Dashboard accessibility verification

**Tomorrow's Priorities:**

1. Verify Rook Ceph cluster health (mon pods running, OSDs created)
2. Test dashboard accessibility at `https://rook-ceph.prod.k3s.annino.cloud`
3. Debug mon pod scheduling if still pending
